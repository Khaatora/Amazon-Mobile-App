enum LoadingState{
  init,
  loading,
  loaded,
  error,
}

enum UserBottomNavScreen{
  a,
  b,
  c
}

enum AdminBottomNavScreen{
  Home,
  Orders,
  Analytics,

}
enum MenuItem{
  wishlists,
  logout
}

enum UserType{
  admin,
  user
}

enum CartLoadingState{
  init,
  adding,
  removing,
  done,
  error,
}